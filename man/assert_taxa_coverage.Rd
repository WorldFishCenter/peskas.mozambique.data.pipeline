% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-taxa.R
\name{assert_taxa_coverage}
\alias{assert_taxa_coverage}
\title{Fail the run when a taxon loses its length-weight coefficients}
\usage{
assert_taxa_coverage(
  taxa_list,
  lw,
  exempt = c("MZZ", "CRA", "CUX", "AND", "NAI", "ADT", "CJV", "CWC", "ECG", "EFZ", "EJX",
    "GQT", "GQV", "ICZ", "NUH", "OCN", "OIC", "PEJ", "PKF", "RDR", "TCI", "TEC", "UVG",
    "YFK", "EFB", "EFN", "HMP", "KAK", "PKV", "QCY", "RMB")
)
}
\arguments{
\item{taxa_list}{Character vector of FAO 3-alpha codes requested.}

\item{lw}{The \code{lw} table from \code{\link[=getLWCoeffs]{getLWCoeffs()}}, after any manually curated
coefficients have been bound on.}

\item{exempt}{Codes that carry no coefficients today. This is a \strong{baseline,
not a whitelist}: it records the taxa that were already uncovered when the
check was introduced (measured 2026-09-06 against FishBase 25.04 /
SeaLifeBase 24.07 over the live KoBo data for both forms, 257 of 288 codes
resolving), so that any \emph{new} loss fails the run. \code{CJX} (\emph{Caesionidae}) and
\code{PWT} (\emph{Scaridae}) are deliberately absent — they resolve at 25.04 and are
the two codes that break at 26.06, so a release move fails here.
Shrinking this list is follow-up work; each group below is a separate fix.

\describe{
\item{Not a taxon}{\code{MZZ} (\emph{Actinopterygii}, "marine fishes nei") is
dropped by \code{\link[=get_fao_groups]{get_fao_groups()}} before the search runs.}
\item{A rank the matcher cannot search}{\code{\link[=match_species_from_taxa]{match_species_from_taxa()}}
handles species, genus, family and order. \code{CRA} ("marine crabs nei")
is the infraorder \emph{Brachyura}, and SeaLifeBase carries no rank between
order \emph{Decapoda} and family; \code{CUX} ("sea cucumbers nei") is the class
\emph{Holothuroidea}, whose 1,133 species span 8 orders and 24 families.
Aliasing either means deciding which families Mozambique lands, so
both are left here rather than guessed at in
\code{\link[=taxa_search_aliases]{taxa_search_aliases()}}. \code{CUX} is the largest single loss in the list,
at 972 Lurio rows.}
\item{Wrong reference name}{The ASFIS name does not describe the animal
landed in Mozambique, so the area 51 filter correctly removes it.
\code{AND} (\emph{Tylosurus acus}) and \code{NAI} (\emph{Naso lituratus}) both name
species absent from FAO 51.}
\item{No published coefficients}{The species resolves and does occur in
FAO 51, but FishBase or SeaLifeBase carries no length-weight pair for
it in any length type. There is nothing to convert and nothing to
alias; the measurement does not exist. \code{ADT}, \code{CJV}, \code{CWC}, \code{ECG},
\code{EFZ}, \code{EJX}, \code{GQT}, \code{GQV}, \code{ICZ}, \code{NUH}, \code{OCN}, \code{OIC}, \code{PEJ}, \code{PKF},
\code{RDR}, \code{TCI}, \code{TEC}, \code{UVG} and \code{YFK} — 19 codes, and the bulk of this
baseline. All but \code{EJX} and \code{OCN} are FishBase.}
\item{No usable length type}{Published (a, b) pairs exist, but in a
length type \code{\link[=get_length_conversions]{get_length_conversions()}} cannot restate on a
total-length basis, because FishBase's POPLL table carries no
proportional length-length fit for that species. \code{HMP} is standard
length, \code{PKV} and \code{QCY} fork length, \code{RMB} disc width (a manta, which
POPLL does not relate to TL at all), and \code{EFB}, \code{EFN} and \code{KAK} carry
a pair with \strong{no length type recorded}, which cannot be converted
from. The conversion did recover 18 other codes here, so this group is
what is left after it.}
}}
}
\value{
\code{lw}, invisibly.
}
\description{
The FishBase read is a live network read of a remote parquet dataset. A new
release therefore reaches the pipeline the moment a container is rebuilt,
with no code change. Release 26.06 dissolved \code{Caesionidae} into \code{Lutjanidae}
and \code{Scaridae} into \code{Labridae}; both family names survive with \strong{zero
species} in them, so any taxon whose reference name is one of those families
expands to nothing and gets no coefficients.

Nothing fails on its own when that happens: \code{\link[=calculate_catch_adnap]{calculate_catch_adnap()}}
left-joins
the coefficients, so a taxon with no \verb{(lw_a, lw_b)} pair yields \code{NA} weight,
and \code{NA} sums to zero. The taxon disappears from the portal and the run stays
green. This turns that silence into a failed job.
}
\section{Porting}{
 The check transfers unchanged, but \code{exempt} is
country-specific. Run once against the country's own taxa list and record
whatever it reports as the starting baseline.
}

\keyword{mining}
\keyword{preprocessing}
