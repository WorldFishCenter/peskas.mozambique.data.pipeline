% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-taxa.R
\name{taxa_search_aliases}
\alias{taxa_search_aliases}
\title{Search names that override the ASFIS reference name}
\usage{
taxa_search_aliases()
}
\value{
A tibble of \code{a3_code}, \code{scientific_name} and \code{rank}. Several rows
may share an \code{a3_code}; all of them are searched.
}
\description{
A few ASFIS reference names match nothing in the taxonomic backbone, so the
taxon is dropped, gets no coefficients, and every catch row of it weighs
\code{NA} -- which sums to zero. This table substitutes a name that does match.
Each entry is a correction to the \emph{reference data}, not to FishBase.
}
\details{
Every row was derived by looking the ASFIS name up in the FishBase or
SeaLifeBase synonym table for the pinned release and taking the accepted
name it points to. Three groups:

\describe{
\item{Synonyms}{Names valid when ASFIS was written and since moved to
another genus. The carangids account for most of them -- \emph{Carangoides}
was split across \emph{Ferdauia}, \emph{Platycaranx}, \emph{Atropus} and \emph{Turrum} --
and the cuttlefish for the rest, \emph{Sepia} having been split across
\emph{Rhombosepion}, \emph{Ascarosepion} and \emph{Acanthosepion}. \code{VMX} is
\emph{Valamugil}, a genus the backbone no longer carries at all; its species
were split across \emph{Osteomugil} and \emph{Moolgarda} (6 species each at
release 25.04), so both are searched. \emph{Crenimugil} also absorbed some
but carries 0 species in the backbone, so listing it would only produce
a standing unmatched-name warning.}
\item{Spellings}{\code{ESR}, \code{PKT}, \code{RPO}, \code{SYQ}, \code{ZEV} and \code{LGE} differ from
the accepted name by an epithet ending or a doubled consonant.}
\item{Broken ASFIS strings}{\code{HES} is truncated in the reference table --
\verb{Herklotsichthys quadrimaculat.} -- and \code{GRX} carries a parenthetical,
\verb{Haemulidae (=Pomadasyidae)}, whose embedded space makes
\code{\link[=process_species_list]{process_species_list()}} read a family name as a species. Both are
substituted with the string the backbone actually holds; \code{Pomadasyidae}
carries 0 species against 138 for \code{Haemulidae}.}
}

\code{GQV} is \emph{Plectorhinchus orientalis}, which the synonym table points at two
species: it is a \code{synonym} of \emph{P. vittatus} and a \verb{misapplied name} for
\emph{P. picus}. Only the former is used.

Two codes are deliberately absent, because choosing a target means deciding
which families Mozambique lands rather than reading a synonym off a table.
Both are a rank \code{\link[=match_species_from_taxa]{match_species_from_taxa()}} cannot search: \code{CRA} ("marine
crabs nei") is the infraorder \emph{Brachyura}, and SeaLifeBase carries no rank
between order \emph{Decapoda} and family; \code{CUX} ("sea cucumbers nei") is the
class \emph{Holothuroidea}, whose 1,133 species span 8 orders and 24 families.
They stay in the \code{\link[=assert_taxa_coverage]{assert_taxa_coverage()}} baseline instead.
}
\section{Porting}{
 Country-specific. The mechanism transfers unchanged; the
rows do not. Rebuild the table for each country's own taxa list.
}

\keyword{mining}
\keyword{preprocessing}
